# GEN_VIO
这是一个针对自动驾驶汽车和道路场景所开发的通用视觉惯性里程计（GVIO），其能实现同时对汽车的定位和对场景中其他动态物体的3D跟踪（2D跟踪和三维运动估计）。
项目中用到了三个神经网络模型YOLOv8、RapidFLOW和DeepPruner，分别用于实现物体检测与分割、密集光流估计 和 立体匹配。
如果您有意使用此代码，需要在/TensorRT_engien路径下放置训练后的此三个模型的oonx文件，并修改/src/GPUProcess/下的相关配置文件（如果改变了模型的输入输出变量名、维度等）。
